// Re-exports desde formatters.js — todos los imports existentes siguen funcionando.
export {
  DAYS_ES,
  DAYS_ABBR,
  MONTHS_ES,
  getWeekDays,
  formatHour,
  isSameDay,
  getMonthLabel,
  getInitials,
} from './formatters'
